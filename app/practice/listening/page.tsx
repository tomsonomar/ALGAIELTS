'use client';import {useState} from 'react';import {Play,Pause,Volume2,CheckCircle2} from 'lucide-react';import {AppShell,Button,Card} from '@/components/ui';
export default function Listening(){const [playing,setPlaying]=useState(false);const [done,setDone]=useState(false);const [answer,setAnswer]=useState('');return <AppShell title="Listening practice"><div className="mb-7"><p className="text-xs uppercase tracking-[.18em] text-blue">Listening · Section 2</p><h1 className="mt-2 text-3xl font-semibold">A new community garden</h1><p className="mt-2 text-sm text-soft">Listen for the speaker’s purpose and key details.</p></div><Card className="mx-auto max-w-3xl p-6"><div className="rounded-xl bg-[#151d2b] p-6"><div className="flex items-center gap-4"><button onClick={()=>setPlaying(!playing)} className="grid h-12 w-12 place-items-center rounded-full bg-blue">{playing?<Pause size={18} fill="currentColor"/>:<Play size={18} fill="currentColor"/>}</button><div className="flex-1"><div className="flex justify-between text-xs text-soft"><span>{playing?'Playing demo audio':'Ready to play'}</span><span>01:28 / 02:40</span></div><div className="mt-3 h-1.5 rounded-full bg-line"><div className={`h-full rounded-full bg-blue ${playing?'w-1/2':'w-1/4'}`}/></div></div><Volume2 className="text-soft" size={18}/></div><p className="mt-5 text-xs text-soft">Demo transcript audio is represented by this player. The audio source is intentionally injectable later without changing the exercise UI.</p></div><div className="mt-8"><p className="text-xs text-blue">Question 1 of 4 · Multiple choice</p><h2 className="mt-2 text-lg font-semibold">What is the main reason the garden was created?</h2><div className="mt-5 space-y-2">{['To provide a place for local food production','To teach children about wild animals','To make the neighbourhood more attractive'].map(o=><label key={o} className={`flex gap-3 rounded-lg border p-4 text-sm ${answer===o?'border-blue bg-blue/10':'border-line'}`}><input type="radio" name="listen" onChange={()=>setAnswer(o)}/>{o}</label>)}</div><Button onClick={()=>setDone(true)} className="mt-7 w-full">Submit answer</Button>{done&&<div className="mt-5 flex items-center gap-2 rounded-lg bg-mint/10 p-4 text-sm text-mint"><CheckCircle2 size={17}/> Nice work — the speaker’s purpose was local food production.</div>}</div></Card></AppShell>}
