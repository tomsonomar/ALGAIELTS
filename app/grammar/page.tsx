'use client';import {useState} from 'react';import {CheckCircle2} from 'lucide-react';import {AppShell,Badge,Button,Card,SectionHeader} from '@/components/ui';import {grammar} from '@/lib/data';
export default function Grammar(){const [active,setActive]=useState(0);const [choice,setChoice]=useState('');const [checked,setChecked]=useState(false);const g=grammar[active];return <AppShell title="Grammar lab"><SectionHeader eyebrow="Grammar lab" title="Make accuracy automatic." copy="Short explanations, useful examples and one quick check for each high-impact topic."/><div className="grid gap-6 lg:grid-cols-[280px_1fr]"><Card className="h-fit p-3">{grammar.map((x,i)=><button onClick={()=>{setActive(i);setChecked(false);setChoice('')}} className={`mb-1 w-full rounded-lg p-4 text-left ${active===i?'bg-blue/15 text-white':'text-soft hover:bg-white/5'}`} key={x.title}><p className="text-sm font-semibold">{x.title}</p><p className="mt-1 text-xs">{x.tag}</p></button>)}</Card><Card className="p-7"><Badge>{g.tag}</Badge><h2 className="mt-5 text-2xl font-semibold">{g.title}</h2><p className="mt-4 max-w-2xl text-sm leading-7 text-soft">{g.text}</p><div className="mt-7 grid gap-3">{g.examples.map(x=><div className="rounded-lg bg-[#151d2b] p-4 text-sm" key={x}>{x}</div>)}</div><div className="mt-8 border-t border-line pt-7"><p className="text-xs uppercase tracking-[.15em] text-blue">Quick check</p><p className="mt-3 text-sm">Choose the best option: “If cities invest more, congestion ___.”</p><div className="mt-4 flex flex-wrap gap-2">{['will decline','would declined','declines'].map(x=><button onClick={()=>setChoice(x)} className={`rounded-lg border px-3 py-2 text-sm ${choice===x?'border-blue bg-blue/10':'border-line'}`} key={x}>{x}</button>)}</div><Button className="mt-5" onClick={()=>setChecked(true)}>Check answer</Button>{checked&&<p className={`mt-4 flex items-center gap-2 text-sm ${choice==='will decline'?'text-mint':'text-red-300'}`}><CheckCircle2 size={15}/>{choice==='will decline'?'Correct — this is the first conditional.':'Not quite. Try “will decline”.'}</p>}</div></Card></div></AppShell>}
