'use client';import {useState} from 'react';import {ArrowRight,CheckCircle2,Clock3} from 'lucide-react';import {AppShell,Badge,Button,Card,SectionHeader} from '@/components/ui';
export default function MockTests(){const [started,setStarted]=useState(false);const [done,setDone]=useState(false);return <AppShell title="Mock tests"><SectionHeader eyebrow="Test centre" title="Practise the full experience." copy="Use realistic timing and original content to build test-day confidence."/><div className="grid gap-5 lg:grid-cols-3"><Card className="border-blue/40 p-7 lg:col-span-2"><Badge>Recommended</Badge><h2 className="mt-5 text-2xl font-semibold">Full IELTS Academic mock</h2><p className="mt-3 max-w-xl text-sm leading-7 text-soft">All four sections in one sitting, with a clear breakdown at the end. Pause between sections when you need to.</p><div className="mt-7 flex flex-wrap gap-5 text-xs text-soft"><span><Clock3 className="mr-1 inline" size={14}/>2h 45m</span><span>4 sections</span><span>Academic</span></div><Button className="mt-8" onClick={()=>setStarted(true)}>{started?'Continue test':'Start mock test'} <ArrowRight size={15}/></Button>{started&&<div className="mt-7 rounded-xl bg-blue/10 p-4 text-sm"><p className="font-semibold">{done?'Mock complete':'Diagnostic mode is ready.'}</p><p className="mt-1 text-xs text-soft">{done?'Your estimated band is 6.5. Review your progress dashboard for the breakdown.':'Start with Reading. You will receive a demo result after the first section.'}</p>{!done&&<Button className="mt-4" onClick={()=>setDone(true)}>Complete demo section</Button>}</div>}</Card><Card className="p-7"><p className="text-xs text-soft">Your mock history</p><p className="mt-3 text-4xl font-semibold">2</p><p className="mt-1 text-sm text-soft">tests completed</p><div className="mt-8 border-t border-line pt-5"><p className="text-xs text-soft">Latest result</p><p className="mt-2 text-xl font-semibold">Band 6.5</p><p className="mt-1 text-xs text-mint">+0.5 from first test</p></div></Card></div><h2 className="mb-5 mt-12 text-xl font-semibold">Section practice tests</h2><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{['Reading','Listening','Writing','Speaking'].map((x,i)=><Card className="p-5" key={x}><p className="text-xs text-blue">Section {i+1}</p><h3 className="mt-3 font-semibold">{x} test</h3><p className="mt-2 text-xs text-soft">{i<2?'60 minutes · 40 questions':'60 minutes · 2 tasks'}</p><Button variant="ghost" className="mt-4 px-0" href={`/practice/${x.toLowerCase()}`}>Open test <ArrowRight size={14}/></Button></Card>)}</div></AppShell>}
